spring:
  application:
    name: spring.starter

  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: test

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update


